/** Reads the selected field from the form and resolves the modal's promise */
export declare function handleSave(contentContainer: HTMLElement, attributes: ComponentAttribute[], resolve: (result: ModalResult | null) => void, hideFn: () => void): void;
