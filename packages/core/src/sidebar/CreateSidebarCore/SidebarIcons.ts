import { svgs } from '../../icons/svgs';

/* Maps every component name to its SVG icon string */
export const SIDEBAR_ICONS: { [key: string]: string } = {
  button: svgs.button,
  header: svgs.header,
  image: svgs.image,
  video: svgs.video,
  text: svgs.text,
  container: svgs.container,
  twoCol: svgs.twocol,
  threeCol: svgs.threecol,
  table: svgs.table,
  landingpage: svgs.landing,
  link: svgs.hyperlink,
};
